<!DOCTYPE html>
<html>
  <head>
    <title>Omniauth</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% flash.each do |key, value| %>
      <%= value %>
    <% end %>

    <% current_user.each do |current_user|%>
      <% if current_user[:user] %>
        <ul class = 'session' >
          <ul>
            <li><%= current_user[:user][:name] %></li>
            <li><%= current_user[:user][:provider] %></li>
            <li><%= current_user[:user][:uid] %></li>
            <li><%= current_user[:user][:location] %></li>
            <li><%= current_user[:user][:image_url] %></li>
            <li><%= current_user[:user][:url] %></li>
          </ul>
          <li><%= image_tag current_user[:user][:image_url], alt: current_user[:user][:name] %></li>
          <li><%= link_to 'Log out', logout_path(type: "#{current_user[:type]}"), method: :delete %></li>
        </ul>
      <% else %>
        <ul class = ''>
          <li><%= link_to "#{current_user[:type]}", "auth/#{current_user[:type]}" %></li>
        </ul>
      <% end %>
    <% end %>
  </body>
</html>
